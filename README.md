# Proof of Authorship

Cryptographic proof ledger. Each entry contains a signed Git commit hash and timestamp from a private repository, proving that specific content existed at that point in time.

No content is disclosed. Verification requires access to the original repository.
